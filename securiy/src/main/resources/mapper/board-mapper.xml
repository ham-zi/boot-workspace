<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.ds.security.board.model.dao.BoardMapper">
  
  
  	<select id="findAll" resultType="BoardDto">
  		SELECT
  		       BOARD_NO
  		     , REF_MNO
  		     , BOARD_TITLE
  		     , BOARD_CONTENT
  		     , CREATE_DATE
  		     , EXIST_FILE
  		     , VIEWS
  		     , STATUS
  		  FROM
  		       DS_BOARD
  		 WHERE
  		       STATUS = 'Y'
  		 ORDER
  		    BY
  		       BOARD_NO DESC
  		OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY
  	</select>
  
 	<insert id="save">
 	<selectKey order="AFTER" resultType="Long" keyProperty="boardNo">
 		SELECT MAX(BOARD_NO) FROM DS_BOARD
 	</selectKey>
 		INSERT
 		  INTO
 		       DS_BOARD
 		VALUES
 		       (
			   SEQ_DS_BNO.NEXTVAL
			 , #{refMno}
			 , #{boardTitle}
			 , #{boardContent}
			 , SYSDATE
			 , #{axistFile}
			 , 0
			 , 'Y'
			   )
 	</insert>
 	
 	<insert id="saveFile">
 		INSERT
 		  INTO
 		       DS_FILE
 		VALUES
 		       (
			   SEQ_DS_FNO.NEXTVAL
			 , #{refBno}
			 , #{originName}
			 , #{changeName}
			 , #{filePath}
			 , #{fileLevel}
			   )
 	</insert>
 	
  </mapper>